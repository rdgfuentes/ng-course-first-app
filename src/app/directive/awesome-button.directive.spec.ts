import { AwesomeButtonDirective } from './awesome-button.directive';

describe('AwesomeButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AwesomeButtonDirective();
    expect(directive).toBeTruthy();
  });
});
